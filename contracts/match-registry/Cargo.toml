[package]
name = "match-registry"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
miden = { version = "0.12" }

[package.metadata.component]
package = "miden:match-registry"

[package.metadata.component.target.dependencies]

[package.metadata.miden]
project-kind = "account"
supported-types = ["RegularAccountImmutableCode"]
